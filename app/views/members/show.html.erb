<% if @member.is_pet %>
<h1>Pet member</h1>
<% end %>
<%= @member.name %>
<%= link_to 'Add event', new_user_member_event_path(@user, @member) %>
<% @member.events.each do |event| %>
  <li><%= link_to event.title, user_member_event_path(@user, event) %></li>
<% end %>
