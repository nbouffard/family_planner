<%= @user.email %>
<% @user.members.each do |member| %>
  <ul>
    <li><%= link_to member.name, user_member_path(@user, member) %></li>
  </ul>
<% end %>
<%= link_to "+ member", new_user_member_path(@user) %>
